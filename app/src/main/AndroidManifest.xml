<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.restaurantes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".reservaciones"></activity>
        <activity android:name=".totalCompras" />
        <activity android:name=".menuBebidas" />
        <activity android:name=".menuAlimentos" />
        <activity android:name=".Menu" />
        <activity android:name=".menuReserOrden" />
        <activity android:name=".registroUsuario" />
        <activity android:name=".registroU" />
        <activity android:name=".menu_alimentos" />
        <activity android:name=".menu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>